# Local raw data and bulky FITS products. These are reproducible from the
# shared Google Drive/raw archive and are too large for ordinary GitHub pushes.
raw/
*.fit
*.fits
*.fts

# Bulky diagnostic overlays. Final figures/CSVs outside qc/ are kept tracked.
analysis/project/qc/

# Python/runtime caches
__pycache__/
*.py[cod]
.venv/
.matplotlib/

# OS/editor noise
.DS_Store

# LaTeX build byproducts. Keep the .tex source and compiled PDF if desired.
*.aux
*.log
*.out
*.toc
*.nav
*.snm
*.fls
*.fdb_latexmk
*.synctex.gz

# Local course/literature PDFs used as references; commit citations/indexes
# instead of redistributing source PDFs.
Preparation of Oral Presentations and Project Reports 2026.pdf
references/am_cvn/*.pdf
